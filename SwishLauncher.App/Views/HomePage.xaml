<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SwishLauncher.App.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
<<<<<<< HEAD
=======
    xmlns:local="using:SwishLauncher.App.Views"
>>>>>>> 9706f627a483bf1c8f3594c82126f8c90ca9edc6
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

<<<<<<< HEAD
    <Grid RowDefinitions="Auto,*,Auto" Padding="48,32">

        <!-- Hero / Greeting -->
        <StackPanel Grid.Row="0" Spacing="8">
            <TextBlock
                Text="{x:Bind ViewModel.Greeting, Mode=OneWay}"
                Style="{ThemeResource TitleLargeTextBlockStyle}"/>
            <TextBlock
                Text="{x:Bind ViewModel.FeaturedTitle, Mode=OneWay}"
                Style="{ThemeResource SubtitleTextBlockStyle}"
                Opacity="0.7"/>
        </StackPanel>

        <!-- Main content area (placeholder) -->
        <Border
            x:Name="ContentRegion"
            Grid.Row="1"
            Margin="0,24"
            CornerRadius="12"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            UseSystemFocusVisuals="True"
            IsTabStop="True">
            <TextBlock
                Text="Featured content will appear here"
                Style="{ThemeResource BodyTextBlockStyle}"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Opacity="0.5"/>
        </Border>

        <!-- Footer / Actions -->
        <StackPanel
            x:Name="ActionsRegion"
            Grid.Row="2"
            Orientation="Horizontal"
            Spacing="12"
            UseSystemFocusVisuals="True">

            <Button
                Content="Refresh"
                Command="{x:Bind ViewModel.RefreshCommand}"
                IsEnabled="{x:Bind ViewModel.IsBusy, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}"
                Style="{ThemeResource AccentButtonStyle}"/>

            <ProgressRing
                IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}"
                Width="24" Height="24"/>
        </StackPanel>

=======
    <Grid>
        <TextBlock
            Text="Home"
            Style="{ThemeResource TitleTextBlockStyle}"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"/>
>>>>>>> 9706f627a483bf1c8f3594c82126f8c90ca9edc6
    </Grid>
</Page>
